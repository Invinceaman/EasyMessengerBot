# required fields
# the user agent is an example user agent, ideally extract the user agent from a browser that you use your account frequently on
FB_EMAIL="{your facebook account email}"
FB_PASS="{your facebook account password}"
USER_AGENT="{desired user agent}"
